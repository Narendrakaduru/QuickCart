const fs = require('fs'); const mongoose = require('mongoose'); const Product = require('./models/Product'); require('dotenv').config({path: './.env'}); const path = './data/products.json'; let products = require(path); const featureIndices = [0, 1, 4, 10, 16, 23, 29, 36]; products = products.map((p, index) => { if (featureIndices.includes(index)) { return { ...p, isFeatured: true }; } return { ...p, isFeatured: false }; }); fs.writeFileSync(path, JSON.stringify(products, null, 2)); mongoose.connect(process.env.MONGO_URI).then(async () => { await Product.updateMany({}, { $set: { isFeatured: false } }); for (let i of featureIndices) { const title = products[i].title; await Product.updateOne({ title: title }, { $set: { isFeatured: true } }); } console.log('Successfully updated JSON and DB with featured products.'); process.exit(0); }).catch(e => { console.error(e); process.exit(1); });
